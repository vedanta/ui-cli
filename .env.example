# UniFi Site Manager API Configuration
# =====================================
# Get your API key from: https://unifi.ui.com
# Navigate to Settings > API and create a new key.

# Required: Your UniFi API key (displayed only once when created)
UNIFI_API_KEY=your-api-key-here

# Optional: API base URL (defaults to stable v1 API)
# UNIFI_API_URL=https://api.ui.com/v1

# Optional: Request timeout in seconds (default: 15)
# UNIFI_TIMEOUT=15


# Local Controller Configuration (for ./ui local commands)
# ========================================================
# Direct connection to your UniFi Controller (UDM, Cloud Key, self-hosted)

# Controller URL (required for local commands)
# UNIFI_CONTROLLER_URL=https://192.168.1.1

# Controller credentials (required for local commands)
# UNIFI_CONTROLLER_USERNAME=admin
# UNIFI_CONTROLLER_PASSWORD=yourpassword

# TOTP code for MFA-enabled accounts (UDM-style controllers only)
# Codes expire in ~30s; only needed for the initial login since the
# session is cached for ~24 hours afterwards.
# UNIFI_CONTROLLER_TOTP=123456

# Site name (default: "default")
# UNIFI_CONTROLLER_SITE=default

# SSL verification - set to false for self-signed certificates (default: false)
# UNIFI_CONTROLLER_VERIFY_SSL=false


# CLI Behavior
# ============

# Disable spinner animations (useful for CI/CD, scripts, or logging)
# Also auto-disabled when CI=true or NO_COLOR is set
# UNIFI_NO_SPINNER=false
